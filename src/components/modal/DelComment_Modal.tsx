import React, { useState } from 'react'
import { Button, Modal } from 'react-bootstrap'
import BlogsService from '../../services/BlogsService'

const DelComment_Modal = () => {
    return (
        <div>
            ยังไม่เชื่อม
        </div>
    )
}